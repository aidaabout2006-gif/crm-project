<!-- <!DOCTYPE html>
<html lang="fa">
<head>
  <meta charset="UTF-8">
  <title>مدیریت مشتری‌ها</title>
 
</head>
<body class="min-h-screen bg-gradient-to-tr from-blue-700 to-purple-700 flex justify-center items-center p-6">

<div class="w-full max-w-5xl bg-white rounded-3xl shadow-2xl p-8">

  <h1 class="text-3xl font-bold text-gray-800 mb-8 text-center">
    مدیریت مشتری‌ها
  </h1>
  <style>
     table { border-collapse: collapse; width: 100%; }
        th, td { border: 5px solid #ccc; padding: 10px; text-align: center; }
        th { background: #f5f5f5; }
        form { margin: 5; }
  </style> -->

  <!-- افزودن مشتری -->
  <!-- <div class="bg-gray-50 rounded-2xl p-6 mb-10">
    <h2 class="text-xl font-semibold mb-4 text-gray-700">افزودن مشتری جدید</h2>
   
<form class="w-full max-w-md" action="../customers/add" method="post">      <input
        type="text"
        name="full_name"
        placeholder="نام کامل"
        required
        class="border rounded-xl px-4 py-2 focus:ring-2 focus:ring-indigo-400 outline-none"
      >

      <input
        type="text"
        name="phone"
        placeholder="شماره تماس"
        class="border rounded-xl px-4 py-2 focus:ring-2 focus:ring-indigo-400 outline-none"
      >

      <input
        type="number"
        name="group_id"
        placeholder="آیدی گروه"
        class="border rounded-xl px-4 py-2 focus:ring-2 focus:ring-indigo-400 outline-none"
      >

      <button
        type="submit"
        class="md:col-span-3 bg-indigo-600 text-white py-2 rounded-xl font-semibold hover:bg-indigo-700 transition"
      >
        افزودن مشتری
      </button>
        <a href="/groups" class="text-blue-500 hover:underline">ورود به صفحه گروه ها</a>
      
    </form>
  </div> -->

  <!-- لیست مشتری‌ها -->
  <!-- <h2 class="text-xl font-semibold mb-4 text-gray-700">لیست مشتری‌ها</h2>

  <div class="overflow-x-auto">
    <table class="w-full border border-gray-200 rounded-xl overflow-hidden">
      <thead class="bg-gray-100">
        <tr>
          <th class="p-3 text-center">ID</th>
          <th class="p-3 text-center">نام</th>
          <th class="p-3 text-center">شماره</th>
          <th class="p-3 text-center">گروه</th>
          <th class="p-3 text-center">عملیات</th>
        </tr>
      </thead>
      <tbody>

        <% if (customers.length === 0) { %>
          <tr>
            <td colspan="5" class="p-4 text-center text-gray-500">
              مشتری‌ای ثبت نشده
            </td>
          </tr>
        <% } %>

        <% customers.forEach(c => { %>
          <tr class="border-t text-center hover:bg-gray-50 transition">
            <td class="p-2"><%= c.id %></td>
            <td class="p-2"><%= c.full_name %></td>
            <td class="p-2"><%= c.phone %></td>
            <td class="p-2"><%= c.group_id %></td>
            <td class="p-2 space-x-2 space-x-reverse">

              <form action="/customers/delete" method="POST" class="inline">
                <input type="hidden" name="id" value="<%= c.id %>">
                <button class="bg-red-500 text-white px-3 py-1 rounded-lg hover:bg-red-600">
                  حذف
                </button>
              </form> -->

              <!-- <a
                href="/customers/edit/<%= c.id %>"
                class="bg-blue-500 text-white px-3 py-1 rounded-lg hover:bg-blue-600"
              > -->
               
              <!-- </a>
               <form action="/customers/update" method="POST" class="inline">
                <input type="hidden" name="id" value="<%= c.id %>">
                <button class="bg-red-500 text-white px-3 py-1 rounded-lg hover:bg-red-600">
                 ویرایش
                </button>
              </form>

            </td>
          </tr>
        <% }) %>

      </tbody>
    </table>
  </div>

</div>

</body>
</html> -->

<!DOCTYPE html>
<html lang="fa">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>مدیریت مشتری‌ها</title>
<style>
* { box-sizing: border-box; font-family: sans-serif; }

body { margin: 0; background: #f4f4f5; }

.layout { display: flex; min-height: 100vh; }

/* ===== Sidebar ===== */
.sidebar {
  width: 250px;
  background: linear-gradient(180deg, #4f46e5, #4338ca);
  color: white;
  padding: 1.5rem 1rem;
  transition: transform 0.3s ease;
}

.sidebar h2 { text-align: center; margin-bottom: 2rem; }

.sidebar a {
  display: block;
  padding: 0.9rem 1rem;
  border-radius: 12px;
  text-decoration: none;
  color: white;
  margin-bottom: 0.6rem;
  transition: background 0.2s;
}

.sidebar a:hover { background: rgba(255,255,255,0.2); }


.content { flex: 1; padding: 1.5rem; }

.topbar { display: flex; align-items: center; gap: 1rem; margin-bottom: 1.5rem; }

.menu-btn {
  display: none;
  font-size: 22px;
  background: #4f46e5;
  color: white;
  border: none;
  padding: 10px 14px;
  border-radius: 10px;
  cursor: pointer;
  z-index: 1001;
}


.card {
  background: white;
  padding: 1.5rem;
  border-radius: 16px;
  box-shadow: 0 10px 25px rgba(0,0,0,0.08);
  margin-bottom: 1.5rem;
}

h1 { margin-top: 0; margin-bottom: 1rem; }

.form-group {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

input {
  flex: 1;
  padding: 0.8rem;
  border-radius: 0.8rem;
  border: 1px solid #ddd;
  outline: none;
}

button {
  background: #4f46e5;
  color: white;
  border: none;
  padding: 0.8rem 1.5rem;
  border-radius: 0.8rem;
  cursor: pointer;
}

button:hover { background: #4338ca; }


.table-wrapper {
  overflow-x: auto;
  margin-top: 1rem;
}

table { width: 100%; border-collapse: collapse; min-width: 600px; }

th, td { padding: 0.9rem; text-align: center; border-bottom: 1px solid #eee; }
th { background: #f3f4f6; }

.btn-danger {
  background: #ef4444;
  padding: 0.5rem 1rem;
  border-radius: 0.6rem;
  border: none;
  cursor: pointer;
  color: white;
}

.btn-danger:hover { background: #dc2626; }

.btn-edit {
  background: #0ea5e9;
  padding: 0.5rem 1rem;
  border-radius: 0.6rem;
  border: none;
  cursor: pointer;
  color: white;
  margin-right: 0.3rem;
}

.btn-edit:hover { background: #0284c7; }

.empty { color: #777; padding: 1.5rem; text-align: center; }

/* ===== موبایل ===== */
@media (max-width: 768px) {
  .sidebar {
    position: fixed;
    top: 0;
    left: 0;
    height: 100%;
    transform: translateX(-100%);
    z-index: 1000;
  }

  .sidebar.active { transform: translateX(0); }

  .menu-btn { display: block; }

  .layout { flex-direction: column; }

  .overlay {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.4);
    z-index: 900;
    display: none;
  }

  .overlay.active { display: block; }
}
</style>
</head>
<body>

<div class="layout">

  <!-- Sidebar -->
  <aside class="sidebar">
    <h2>CRM پنل</h2>
    <a href="/dashboard">داشبورد</a>
    <a href="/customers">مشتری‌ها</a>
    <a href="/groups">گروه‌ها</a>
    <a href="/users/logout">خروج</a>
  </aside>

  <!-- Content -->
  <main class="content">

    <div class="topbar">
      <!-- دکمه منوی موبایل داخل topbar -->
      <button class="menu-btn" onclick="toggleMenu()">☰</button>
      <h3>مدیریت مشتری‌ها</h3>
    </div>

    <!-- افزودن مشتری -->
    <div class="card">
      <h1>افزودن مشتری</h1>
      <form action="/customers/add" method="POST" class="form-group">
        <input type="text" name="full_name" placeholder="نام کامل" required>
        <input type="text" name="phone" placeholder="شماره تماس">
        <input type="number" name="group_id" placeholder="آیدی گروه">
        <button type="submit">افزودن</button>
      </form>
    </div>

   
    <div class="card">
      <h1>لیست مشتری‌ها</h1>
      <div class="table-wrapper">
        <table>
          <tr>
            <th>ID</th>
            <th>نام</th>
            <th>شماره</th>
            <th>گروه</th>
            <th>عملیات</th>
          </tr>

          <% if (customers.length === 0) { %>
            <tr>
              <td colspan="5" class="empty">هیچ مشتری‌ای ثبت نشده</td>
            </tr>
          <% } %>

          <% customers.forEach(c => { %>
            <tr>
              <td><%= c.id %></td>
              <td><%= c.full_name %></td>
              <td><%= c.phone %></td>
              <td><%= c.group_id %></td>
              <td>
                <form action="/customers/delete" method="POST" style="display:inline;">
                  <input type="hidden" name="id" value="<%= c.id %>">
                  <button class="btn-danger">حذف</button>
                </form>

                <form action="/customers/edit/<%= c.id %>" method="GET" style="display:inline;">
                  <button class="btn-edit">ویرایش</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </table>
      </div>
    </div>

  </main>
</div>

<div class="overlay" onclick="toggleMenu()"></div>

<script>
function toggleMenu() {
  document.querySelector('.sidebar').classList.toggle('active');
  document.querySelector('.overlay').classList.toggle('active');
}
</script>

</body>
</html>
